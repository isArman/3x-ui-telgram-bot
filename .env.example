# Required — no leading spaces before variable names
BOT_TOKEN=your_telegram_bot_token

# One or more Telegram user IDs, comma-separated
ADMIN_IDS=123456789,987654321

# Optional legacy seed for card details (preferred: set in-bot under پنل ادمین → کارت بانکی)
# CARD_NUMBER=6037-XXXX-XXXX-XXXX
# CARD_HOLDER=Your Name

# SQLite database path (default is fine for Docker)
DATABASE_URL=sqlite+aiosqlite:///data/db.sqlite3

# Encrypts 3x-ui panel password stored in the database.
# Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
SECRET_KEY=

# Verify TLS when connecting to 3x-ui panel (set false only for self-signed certs in dev)
XUI_VERIFY_SSL=true

# Logging: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
