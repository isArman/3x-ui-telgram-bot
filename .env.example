# Optional - Leave empty and configure via /panel in bot (manual mode)
DATABASE_URL=sqlite+aiosqlite:///data/db.sqlite3

# Optional initial 3x-ui panel settings (can also be set by admin via /panel)
XUI_URL=
XUI_USERNAME=
XUI_PASSWORD=
XUI_INBOUND_ID=1
