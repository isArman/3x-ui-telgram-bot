version: "3.8"

services:
  bot:
    build: .
    container_name: vpn_bot
    restart: always
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///data/db.sqlite3
    volumes:
      - ./data:/app/data
